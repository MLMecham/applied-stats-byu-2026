# 05-models.R
# Slide 02: Programming with LLMs (Choosing a model)
# Goal: send the same prompt to several providers and compare answers, latency,
# and token usage.

library(ellmer)

# TODO: send the same prompt through chat_openai(), chat_anthropic(),
# chat_google_gemini(), and chat_ollama()
