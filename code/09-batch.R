# 09-batch.R
# Slide 02: Programming with LLMs (Parallel and batch calls)
# Goal: run the structured recipe extraction across many PDFs in parallel.
# Save the JSON output so we can reuse it in later exercises.

library(ellmer)

# TODO: collect a list of recipe PDFs
# TODO: run extraction in parallel and serialize the results to JSON
